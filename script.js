const input=document.getElementById('search');function filterTools(){const q=(input.value||'').toLowerCase().trim();document.querySelectorAll('#grid article').forEach(a=>a.style.display=(!q||a.dataset.text.includes(q)||a.textContent.toLowerCase().includes(q))?'block':'none')}function quick(q){input.value=q;filterTools();document.getElementById('tools').scrollIntoView({behavior:'smooth'})}input?.addEventListener('input',filterTools);